server:
  port: 8082

spring:
  application:
    name: transactions
  datasource:
    url: jdbc:postgresql://localhost:5433/payflow_transactions
    username: payflow
    password: payflow
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

accounts:
  service:
    base-url: http://localhost:8081
